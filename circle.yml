dependencies:
  pre:
    - echo "The Experiment Factory!"
    - cd /tmp && git clone https://www.github.com/expfactory/expfactory
    - cd expfactory && sudo python setup.py install

test:
  override:
    - echo "The Experiment Factory!"
    - cd $HOME/library && python -m unittest test
